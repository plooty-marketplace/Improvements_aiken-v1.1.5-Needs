contract InteroperableContract {
    public function callExternalContract(address externalContract) {
        // Hypothetical function call to an external contract
        externalContract.call("someFunction");
    }
}